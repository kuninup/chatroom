/*
*项目
*/
需要实现六大类一个包,六大类
Config
含有关于界面的需要的各种常数，双方的IP地址及端口
MainUL
1.实现主界面
2.主方法。
DataHandle:。
对接收到的字节流依照通信协议做各种的处理。
需要注意在视频那一部分要留接口给图像处理小
DataSender
依照通信协议对数据做各种处理然后发送。
Filechoose
发送文件的接口
ActLis
监听鼠标的操作。
ImageProcessing。
调用百度接口实现人类的一些部位的识别，。
一个包:。
修改过的精简版本的五子棋。

/*
*通信协议制定
*/
1代表视频通话
2 字符串
3 代表文件
4 代表五子棋 
完成五子棋需要传输的数据
	XO + i * SIZE - CHESS / 2, YO + j * SIZE - CHESS / 2, CHESS, CHESS
1、	需要i和j
2、	传递一个参数表示棋局是否已经结束
总结一共需要三个数据
用一个大小为5的byte数组存储
byte[0] = 4
byte[1] = ROW
byte[2] = column
byte[3] = 1代表黑棋
byte[3] = 2代表白棋
byte[4] = 3 代表还未分出胜负
byte[4] = 1 代表黑胜利
  